<h1>All Users</h1>

<%= will_paginate %>

<ul id="users-index">
  <% @users.each do |user| %>
    <li>
    	<%= gravatar_for user, size: 50 %>
      <%= link_to user.user_name, user %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>
<p class="pagination"><%= page_entries_info @users %></p>

